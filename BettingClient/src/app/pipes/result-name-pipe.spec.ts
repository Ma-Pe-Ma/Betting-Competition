import { ResultNamePipe } from './result-name-pipe';

describe('ResultNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ResultNamePipe();
    expect(pipe).toBeTruthy();
  });
});
